#!/bin/bash

# ensure all gems are installed
vagrant ssh -c 'cd /vagrant && bundle install --local'

# Restart development server upstart jobs
vagrant ssh -c 'sudo restart app'

# restart spring application preloader
spring stop
